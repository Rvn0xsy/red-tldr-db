name: tscon
tags: [tscon,con]
data: |

  # 切换远程桌面会话

  tscon {<sessionID> | <sessionname>} [/dest:<sessionname>] [/password:<pw> | /password:*] [/v]

  # 切换Session ID 为 2 的桌面会话

  tscon 2

  # 切换到Session ID 为 2 的桌面会话，并传入凭据

  tscon 2 /password:mypass
